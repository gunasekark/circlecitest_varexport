machine:
  python:
    version: 2.7.12
compile:
  override:
     - echo 'hello world'
     - echo $ABC
     - |
        CDE=${ABC}
        echo $CDE
     - 'ls -l ~/'
    # - 'ls -l ~/.aws/'
test:
  override:
     - python nosetest.py
